import React from 'react';
import { View, Text } from 'react-native';

/**
 * 미로그인 사용자 프로필 화면 컴포넌트
 * 로그인하지 않은 사용자가 프로필 화면에 접근했을 때 보여지는 화면
 */
const GuestProfileScreen = () => {
  return (
    <View>
      <Text>미로그인 사용자 프로필 화면</Text>
    </View>
  );
};

export default GuestProfileScreen;
